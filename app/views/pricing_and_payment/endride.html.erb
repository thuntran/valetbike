<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title">
      Help us
    </div>
    
        <form>
    <select name = "station">
        <% @stations.each do |station|%>
        <option><%=station.name%></option>
        <%end%>
    </select>
        <br/>
    <input type="submit" value="select station">
        </form>
        <p>...</p>

    <% if params[:station] %>
        You have finished your ride on bike: <%= (Bike.find(@ride.bike_id)).identifier %>. 
        Ride started at <%= @ride.start_time %> from <%= Station.find(@ride.start_station_id).name %>.<br/>
        Ride finished at <%= @ride.end_time %> at <%= Station.find(@ride.end_station_id).name %>.<br/>
        User = <%= User.find(@ride.user_id).username %><br/>

        Bike's current station = <%=Station.find(Bike.find(@ride.bike_id).current_station.id).name%><br/>
        <br/>
    <% end %>
	</div>
</div>